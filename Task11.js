const r1=require("readline").createInterface({
    input:process.stdin,
    output:process.stdout,
    });

    r1.question("Enter a number:\n",(input)=>{
        let number= parseFloat(input);

        console.log("inetial number:",number);
        
        
        console.log("number:",++number)
        console


     
    });
